import "./style.css";

type ProjectListMenuProps = {
  show: boolean;
};

export default function ProjectListMenu({ show }: ProjectListMenuProps) {
  return <div class="pl-menu__container">menu</div>;
}
